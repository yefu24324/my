<div aria-hidden="true" class="nav-brackets" data-nav-brackets>
  <span class="nav-bracket" data-bracket="left">[</span>
  <span class="nav-bracket" data-bracket="right">]</span>
</div>

<style>
.nav-brackets {
  position: absolute;
  inset: 0;
  pointer-events: none;
  user-select: none;
  opacity: 0;
  transition: opacity 150ms ease;
}

.nav-brackets.is-ready {
  opacity: 1;
}

.nav-bracket {
  position: absolute;
  top: 0;
  left: 0;
  font-family: var(--font-serif, "Newsreader Variable", Georgia, serif);
  font-size: 1rem;
  line-height: 1;
  transform: translate3d(0, 0, 0);
  transition: transform 220ms cubic-bezier(0.77, 0, 0.175, 1);
  will-change: transform;
}

/* 重新测量（窗口尺寸变化等）时不做过渡，避免括号从旧位置滑过来 */
.nav-brackets.is-measuring .nav-bracket {
  transition: none;
}

/* 按站点所有者要求：括号的滑动动画始终播放，不随“减少动态效果”设置降级为瞬移 */

/* 移动端是下拉菜单，不显示括号 */
@media (max-width: 767px) {
  .nav-brackets {
    display: none;
  }
}
</style>
